export enum Routes {
  ADMIN = '/admin-panel',
  LOGIN = '/login',
  REGISTRATION = '/registration',
  STORE = '/',
  BASKET = '/basket',
  PRODUCT = '/product',
}

export enum Roles {
  GUEST = 'guest',
  USER = 'user',
  ADMIN = 'admin',
}